name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install uuid-dev
      run: sudo apt-get install uuid-dev
    - name: build
      run: make
    - name: make tests
      run: make tests_run
